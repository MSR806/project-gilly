node_modules/
dist/
*.log

# Local env + secrets
.env
.env.local

# SQLite operational store
*.db
*.db-shm
*.db-wal
data/

# Bun
.bun/

# UI/runtime-created skills (LocalSkillStore backing files) — keep only the seed skill.
config/skills/*
!config/skills/our-repos/
